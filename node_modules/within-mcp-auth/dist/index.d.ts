/**
 * With.in MCP Auth SDK — public surface.
 *
 * One entry point: `createWithinHandler`. It returns a sealed handler
 * that owns authentication, trial-quota enforcement, tool-call reporting,
 * conversion tracking, session management, transport construction,
 * and network-tool injection. Vendors cannot skip a step because none
 * of the steps are exposed.
 *
 * See README.md for the full integration walkthrough.
 */
export { createWithinHandler } from './middleware.js';
export type { WithinHandler } from './middleware.js';
export type { WithinConfig, WithinTokenClaims, AuthResult, McpServerLike } from './types.js';
export { getNetworkStatusCache } from './network.js';
